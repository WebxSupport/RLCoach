# Normalise line endings — keep LF in the repo so scripts/Dockerfile work on Linux
* text=auto eol=lf
*.svg text eol=lf
*.png binary
*.ico binary
*.zip binary
# The vendored rrrocket ELF binary must NOT be line-ending converted
rrrocket_bin/linux/rrrocket binary
