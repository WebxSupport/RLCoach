# Phoenix loop 2: dense data must open with a coach verdict and one action before exposing telemetry.

- Fresh verifier on the previous screenshot set found the remaining launch risk was decision architecture, not visual polish: Series, Stats, Match Dashboard, and Coaching still made the player interpret too much data.
- Applied the Phoenix rule to the data-heavy surfaces: Check Series, Match Tape, Momentum, and generated Match Dashboard now start with an interpretive coach read, one dominant action, and supporting proof metrics.
- Mobile Today was adjusted so the daily-loop explanation appears before the progress ring; the ring is now evidence for the mission, not unexplained decoration.
- Navigation now uses loop language: Train Today, Check Series, Match Tape, Momentum.
- Post-verifier wording tweak: Today uses "Games logged" and "Open match tape" so the visible step and the CTA agree.
- Verification screenshots for this pass live in `docs/redesign-notes/shots/phoenix-loop2-fix/`.
- Syntax checks passed for extracted inline JavaScript from `static/index.html` and `static/dashboard_template.html`.
